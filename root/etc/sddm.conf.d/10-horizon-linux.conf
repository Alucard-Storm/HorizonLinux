[General]
DefaultSession=hyprland.desktop

[Users]
# Hide the baked-in "horizon" account from the greeter's user list - the
# account still exists and can still be logged into if needed (e.g. from
# a TTY), it just doesn't show up as a face-browser tile.
HideUsers=horizon

[Autologin]
User=liveuser
Session=hyprland.desktop
